Decompose the following code-repo knowledge entry into atomic, self-contained claims. Each claim should be a single decision, rationale, direction, clarification, or observation that stands on its own without requiring the other claims for context.

Preserve specific details: module names, file paths, class names, protocols, data shapes, version numbers, technical terms. Do NOT generalize or summarize — keep original specificity. Preserve ALL claims. When a claim references another module or entity, include enough context that the claim is still meaningful in isolation (e.g., "The auth-service uses JWT tokens signed with RS256" rather than "uses JWT tokens").

Classify each atomic claim as decision, rationale, clarification, direction, observation, or question.
