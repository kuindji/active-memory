For each numbered code-repo entry below, generate 2-3 questions that this entry answers. Include both:
- One specific question that pinpoints this entry's unique content (e.g., "Why does the X module use Y?", "What is the contract between the A service and the B database?")
- One broader question a developer might naturally ask when looking for this information (e.g., "How does auth work in this repo?", "What modules handle payments?")

Cover the main subjects: decisions (why), rationale (why not alternatives), directions (how going forward), clarifications (what a term means in this codebase), observations (current behavior), and open questions. Use concrete module names, file paths, and technical terms from the entry. Return the questions as a single string (semicolons between questions).
