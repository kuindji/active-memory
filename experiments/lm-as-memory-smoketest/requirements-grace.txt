# Aligned to vendor/EasyEdit/requirements.txt, stripped of multimodal / eval-only deps
# (timm, iopath, opencv-python, fairscale, av, qwen_vl_utils, zhipuai, openai,
# sentence-transformers, rouge, matplotlib, nltk, gpustat).
# The ancient datasets==1.18.3 pin is MEMIT-covariance-only; GRACE doesn't need it.
torch==2.9.1
transformers==4.57.1
tokenizers==0.22.1
peft==0.18.0
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2
pandas==2.3.3
pyarrow==18.1.0
sentencepiece>=0.2.0,<0.3
einops==0.8.1
higher==0.2.1
hydra-core==1.3.2
omegaconf==2.3.0
PyYAML==6.0.2
tqdm==4.67.1
importlib-metadata==8.7.0
datasets>=2.14,<3
# EasyEdit itself is vendor-cloned into vendor/EasyEdit (no pyproject.toml upstream)
# and imported via sys.path in src/grace_engine.py.
